class States:
	def __init__(self):
		self.branch = "branch"
		self.basic = "basic"
		self.reject = "reject"

		self.sleep = "sleep"
		self.find = "find"
		self.found = "found"

		self.connect = "connect"
		self.initiate = "initiate"
		self.accept = "accept"
		self.test = "test"
		self.report = "report"
		self.changeroot = "changeroot"
